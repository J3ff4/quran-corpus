import { View } from 'react-native';

/** ponytail: stand-in for Task 3, which moves MushafReader's body in here. */
export function MushafScreen() {
  return <View testID="mushaf-screen" style={{ flex: 1 }} />;
}
